Job Checker 

What simply does is, scrapes data from a website and emails the necessary data to the user every day as scheduled.

To run app on your local machine kindly run 

```$ make up```

To deploy app to heroku please run the command below after logging into heroku

```$ make heroku_deploy``` 

To destory heroku app, simply run 

```make heroku_destroy```